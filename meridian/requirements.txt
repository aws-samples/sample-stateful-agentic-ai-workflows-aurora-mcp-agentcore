# Strands Framework
strands-agents>=0.1.0
strands-agents-tools>=0.1.0

# AWS Services
boto3>=1.34.0
botocore>=1.34.0

# MCP (Model Context Protocol) for Phase 2
mcp>=0.1.0

# LangGraph (Phase 5 — workflow orchestration with checkpointed state)
langgraph>=0.2.0
# Optional: PostgresSaver for durable LangGraph checkpoints in Aurora.
# Requires direct Postgres connectivity (not RDS Data API).
langgraph-checkpoint-postgres>=2.0.0
psycopg[binary]>=3.1.0
psycopg-pool>=3.2.0

# FastAPI Backend
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6  # For file uploads

# Vector operations (for pgvector result parsing)
numpy>=1.24.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# For pretty output
rich>=13.0.0
tabulate>=0.9.0

# Testing
pytest>=7.4.0
hypothesis>=6.100.0
