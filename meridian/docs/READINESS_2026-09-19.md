# Meridian source and demo readiness - 19 September 2026

**Verdict: the repaired local demo passes the executed source and live-service checks; complete hosted presentation readiness is not established.** The hosted web application is older, fresh infrastructure provisioning/teardown was not executed, and human delivery/room checks remain. Workshop Studio is outside the user-confirmed scope. See the [acceptance matrix](ACCEPTANCE_MATRIX_2026-09-19.md), [presenter runbook](PRESENTER_RUNBOOK_2026-09-19.md), [editable presentation](presentation/Meridian-reInvent-chalk-talk.pptx), and [evidence inventory](EVIDENCE_2026-09-19.json).

## Scope and identity

The working repository is `/Users/shayons/Desktop/Workshops/sample-stateful-agentic-ai-workflows-aurora-mcp-agentcore`, branch `main`, remote `origin`, `https://github.com/aws-samples/sample-stateful-agentic-ai-workflows-aurora-mcp-agentcore.git`. Both the initial local HEAD and independent remote lookup resolved to `42c1defce75289ba76ea727da72b59acf1ea33d2`. The user explicitly transferred ownership of the unfinished September 19 repairs, including the already-staged obsolete deck-generator deletion. No other worktree or default workshop track was substituted.

L300 audience; a planned 60-minute slot with 40 minutes core content and 20 minutes discussion/flex. The central takeaway is that conversational context, permission decisions, durable checkpoints and business records have different owners. Required path: Concierge → SQL/MCP/Retrieval/Production capability ladder → Recovery desk → System evidence. Handoff/confirmation and lost-response fault injection are optional depth. The actual worker-replacement proof is a separate SIGKILL execution, not the ordinary browser pause/resume.

Root AGENTS.md and nested AgentCore AGENTS.md were read. No project VOICE.md was found. The imported `~/.Codex/steering/*.md` files are absent; corresponding `~/.claude/steering/` guidance was read as context. PRODUCT.md, DESIGN.md, frontend-design, design-taste, Impeccable, PowerPoint, AWS CDK and CloudFormation guidance informed the review. AWS MCP was available for identity/documentation/live read-only inspection; CLI was used where appropriate. No subagents were used.

AWS identity was verified as the established demo account `619763002613`, region `us-east-1`. The review used an isolated local backend on loopback port 8027 and Vite production preview on 5197. Existing listeners on 8013, 8015 and unrelated frontend/database ports were preserved. No local PostgreSQL substitute was created.

## Repairs and findings

| Severity | Finding and repair | Evidence |
| --- | --- | --- |
| High | Reference MCP callbacks could lose async lifecycle/context; SQL and retrieval reference behavior drifted from the five-phase story. Repaired persistent MCP use, deterministic phase boundaries and read-only Retrieval availability. | MCP/memory/retrieval tests; real comparison, loyalty and retrieval calls |
| High | Unknown loyalty programs returned generated points presented as a balance. Removed this fallback; absent program/balance returns an explicit unavailable result, while configured balances still come from Aurora. | Known/unknown program live lookup and missing-balance regressions |
| High | Loyalty/preference tool access needed the same workload-to-traveler grant and RLS scope as other protected paths. Repaired both reference MCP paths. | Scoped/decoy tests and live RLS probe |
| High | API readiness could hide a protected health failure by falling back to public process liveness. Removed fallback; use `/api/health`, shared bounded requests, and correct local port 8013. | Initially failing health regressions now pass; live health and offline recovery |
| High | Setup omitted migrations, initialization could mutate an existing database, and full seed could destroy prior demo state. Added ordered migrations, empty-state refusal, advisory locking and atomic initialization/seed rollback. Catalog-only refresh preserves traveler history. | Fresh disposable database inside the existing Aurora cluster; schema, nine migrations, seed, rerun refusal, injected rollback and retry all executed; disposable DB removed |
| High | Lifecycle utilities could swallow errors or delete broadly. Cleanup is dry-run by default, fails closed on live cluster/access errors, and retains a secret recovery window. Cluster deletion requires explicit account-scoped apply, preserves final snapshots, and stops on wait failure. Booking release supports an exact ID and transaction/ownership recheck. | Ten lifecycle regressions, safe plan execution and owned booking cleanup; no shared resource teardown |
| Medium | Cedar denials were not consistently recognized when a tool returned an error status without `isError=true`. Repaired Runtime trace classification. | Regression tests, live denials and runtime source-byte parity |
| Medium | Database timestamps could be interpreted in the browser's local timezone. Normalize timestamp fields to explicit UTC without changing money values. | Backend timestamp tests and New York timezone frontend regressions |
| Medium | Same-worker resume was labeled replacement; a new recovery could show a previous Concierge receipt and completed recoveries could show it twice. Use truthful execution labels and only the selected journey's receipt. | Regression tests; browser new recovery has zero receipts, selected completed journey has one |
| Medium | Order API omitted structured remaining-inventory evidence. Added `seats_remaining` from the actual hold receipt. Updated stale live assertions to inspect structured receipts, denied activities and database effects, not model wording. | 106 live database tests; 31-step HTTP demo |
| Medium | Generated AgentCore policy contained nonexistent `CheckAuthorizePermissions`. Added a narrow CDK compatibility aspect removing only that action; required policy-engine permissions and resource identities remain. | Synth assertion and current AWS authorization documentation; no deployment implied |
| Medium | Test environment drifted from the lock; frontend build was not an independent type-check. Restored hash-locked installation, added the required TestClient transport dependency, and added independent type-checking/runtime lint and CDK dependency audits to CI. | Locked installation, lint, type-check, builds and audits |
| Medium | Latest Toronto deck omitted new governance/recovery work and retained illustrative scale, obsolete implementation and expired promotion claims. Updated an editable copy to 25 slides with current architecture, notes, dated captures, explicit tradeoffs and optional branches. | Native PowerPoint rendering, package validation, title/alt-text checks, transcript and presenter runbook |

The non-secret part of new-cluster provisioning now requires explicit account-scoped apply, checks the requested engine, requests encryption/seven-day backups/private instances, and chooses distinct availability zones. **Its inherited password-generation/CLI-argument path has not been replaced or executed.** That remains an infrastructure security/lifecycle blocker, not a completed fix; see below.

## Review and evidence coverage

The review covered first-party frontend surfaces/hooks/API client, FastAPI routers/authentication, agents and MCP tools, memory/RDS Data API access, checkpoint saver, journey/lease/intent code, all migration files, Gateway Lambda and Runtime hooks, scripts, both CDK projects, lockfiles, tests and CI. Review and static checks cover every subsystem; this is not a claim of formal verification or that every possible input was exercised.

The real path is browser HTTP principal → traveler binding → workload grant/RLS → appropriate agent/tool → parameterized Aurora operations → independently re-read result. Production pins caller identity, confirmation and budget outside model output. Cedar runs before Gateway Lambda; Lambda rechecks ownership, inventory and atomic replay terms. Privileged IAM Gateway callers remain a trusted boundary. The sample is a shared single-presenter principal, not a multi-tenant login system. SQL starter routing is deterministic. Exchange rates, traveler persona, catalog and inspiration imagery are demo data. Rerank animations replay returned data, not measured execution timing. Solution briefing diagrams are explanatory static material. No payment, airline reservation or supplier call occurs.

AI checks cover configured phase boundaries, read-only retrieval, actual named tools, memory on/off, grounded results, model-supplied argument pinning, denial preservation, and safe failure labels. They are contract/security tests, not a statistical quality benchmark or exhaustive prompt-injection red team. No online model-judge evaluator is configured.

| Check | Final observed result | Local evidence under `.local/release-review-2026-09-19/` |
| --- | --- | --- |
| Backend lint | PASS; backend, scripts, tests, Runtime and Gateway target | `gates/ruff-final.log` and command transcript |
| Hash-locked offline backend tests | 485 passed, 6 skipped, 110 live tests deselected | `gates/backend-release.log` |
| Live database suite | 106 passed, 4 skipped, 487 deselected; 197.50 s | `gates/database-final.log` |
| Frontend | Lint, independent type-check, 237 tests/36 files, production build PASS | `gates/frontend-final.log` |
| Dependency audits | Python plus frontend/web-CDK/AgentCore-CDK: no known vulnerabilities at execution time | `gates/backend-audit.log`, initial gate logs and final command transcript |
| Infrastructure | Web CDK seven tests; AgentCore CDK one synth test; builds/synth and `agentcore validate` PASS | `gates/web-infra.log`, `agentcore-policy-test.log`, `agentcore-synth-final.log`, `agentcore-validate-final.log` |
| Template lint | No errors; ten W3005 redundant DependsOn warnings, exit 4 retained | `gates/cfn-lint-final.log` |
| Live platform | Runtime/Gateway READY, four tools, Memory ACTIVE, policy ACTIVE/ENFORCE, observability READY | `gates/agentcore-live.log` |
| Full HTTP demonstration | 31 checks PASS; no request exceeded 55 s; 171.6 s total HTTP; cleanup succeeded | `gates/live-e2e-final.json`, `.log` |
| Actual worker death | SIGKILL -9, lease protection, new worker, same hold/expiry, one database record, own rows cleaned | `gates/kill-resume-final.log` |
| Lost committed reply | Deliberately discarded real reply; resumed same intent; one hold/unchanged expiry; own rows cleaned | `gates/lost-response-final.log` |
| Bootstrap/reset | Empty schema, migrations, seed; safe reruns, nonempty refusal, real transaction rollback and retry | `gates/bootstrap-lifecycle-retry.log` and individual command logs |
| Rendered app | Five required surfaces at 1366×768, 1920×1080 and 390px; real SQL, recovery, reload, handoff and confirmation; scoped receipts verified | `browser/` screenshots, DOM and responsive measurements |
| Assets and links | 146 relative Markdown links resolved at check time; images loaded on rendered routes; exact runtime source hashes matched deployed archive | `gates/links.json`, `runtime-parity.json`, browser evidence |
| Secret scan | Source-only Gitleaks scan found two `meridian:PASSWORD` documentation placeholders; reviewed as non-secret. No real credential finding in release sources. | `gates/gitleaks-source.json`; initial broad scan separately retained |

Raw evidence includes timestamps, command outputs, failures and earlier attempts. The committed [evidence inventory](EVIDENCE_2026-09-19.json) records hashes and file timestamps for reproducibility; local logs are ignored because they include workload/resource details. The first live E2E cleanup failed on a conversation foreign key; the cleanup order was fixed, its owned rows were removed, and the entire final E2E run then succeeded. Earlier database tests had stale UUID/price/inventory assertions; failed logs remain. An initial timestamp regression failed after UTC normalization, then passed after the assertion was aligned to the new explicit format. An initial deck package passed schema validation yet PowerPoint found a duplicate title placeholder; native validation caught it and the duplicate was repaired. A final tracked-file test rejected a deployment URL in this report; the URL was removed to comply with repository policy. The first loyalty probe omitted environment initialization and failed before making a database call; its configured retry is recorded separately. These earlier results are not silently relabeled as passes.

The skipped saver tests concern InMemorySaver-specific context-manager/private delta features or synchronous/filter variants outside this application's async saver contract. They are not live successes. Unsuppressed upstream diagnostics remain: MCP/Pydantic unresolved `lifespan` settings warning, Node localStorage experimental warnings in tests, generated CDK deprecation/S3 object-version warnings and W3005 dependencies. Runtime asset object keys are content hashes, but that does not constitute a fresh deployment test.

## Persisted payoff and deployment boundary

Browser journey `jrn_cddc6e6b1e7f`, thread `phase5-5e90ceb8-adc6-4b73-b7ad-fec32d460a54`, checkpoint `1f1b4861-664d-6e0a-8007-667eb2ad18f8`: worker `worker-0ff469d9` paused and resumed. Hold `hold_276e428b4950` was read from Aurora, handed back to Concierge, then confirmed for two travelers, two nights, $3,898. That is **same-worker resume evidence**. Its owned booking, journey and conversation were removed after the final UI/readback checks; cleanup verified absence without touching unrelated records.

Separate SIGKILL journey `jrn_65911cce9cac`, thread `demo-d8dbd1a5b5`: first worker `worker-fd9a1f4e` exited -9; a different worker resumed. Hold `hold_c863973e50c2` remained unique and retained expiry `2026-09-20T00:35:05.372578+00:00`. Separate lost-response journey `jrn_21770802c3d5` retained hold `hold_9aec66e7257e` and its original expiry. Fault-test records were removed after proof; their logs are dated execution evidence, not currently live saved journeys.

AgentCore Runtime `meridianv2_MeridianConcierge-LpDBbFBjsc`, version **14**, was READY. Its deployed archive key was `286a291fb30eae840bb4e8467714f557c855f116f4078e75622ee56b8d909ee0.zip`. All seven first-party Runtime Python files matched the local source byte-for-byte. Gateway `meridianv2-meridian-aurora-temzt21jg0` exposed four tools. Existing Aurora `meridian-demo` reported engine 18.3 with Data API enabled.

**Hosted web does not match final source.** CloudFront distribution `E2Z7IZTFX7YYZJ` has its endpoint recorded only in ignored local deployment evidence; its S3 index references old `index-DGyQg8oy.js`. App Runner still identifies image tag `2c5495b068259535d8ea8a4b0ad2794355dee68f4788f2c216a6ccaa0334187e` from the older deployment. The distribution lacks the response-headers policy now in source. This review neither deployed that web application nor claimed authenticated hosted execution. A source push does not update it.

## Remaining blockers and smallest next actions

1. **Hosted release and authenticated parity - BLOCKED.** Obtain explicit authorization for that target, make the approved secret execution capability available, run the established web release procedure, verify the resulting source/image/static hashes and security headers, then repeat authenticated startup, full demo, denial, readback and recovery. Existing runtime parity is narrower evidence.
2. **New infrastructure provisioning and teardown - BLOCKED.** Root [AGENTS.md](../../AGENTS.md) requires loading `aws-secrets-manager` and runtime `asm-exec` resolution for credential tasks. Neither exact capability was available after discovery. No secret values were fetched. The inherited create-cluster credential path and hosted publisher require review/replacement under that approved workflow before execution. New provisioning, partial infrastructure rollback and full teardown were not authorized or tested. Use an explicitly authorized isolated target; retain snapshots and verify deletion by resource ID. Static IAM/template checks do not prove deployer PassRole, service-linked-role creation, boundaries, quotas or fresh-account permissions.
3. **Existing environment security - FAIL for a hardened deployment.** Live Aurora reported storage encryption off, deletion protection off and one-day backup retention. Source changes cannot retrofit encryption. Authorize a snapshot/copy/restore migration plan and retention decision if this environment must meet a hardened release standard. Do not delete or migrate the shared cluster during presenter preflight. Aurora capacity, snapshots, logs, Runtime/Memory, App Runner and CloudFront can continue to incur costs.
4. **Physical/human delivery - BLOCKED.** Perform the runbook's timed human path and actual room/network/projector checks. Laptop/browser checks do not establish these. The final browser zoom/reduced-motion retry was blocked by unavailable browser control (existing tab debugger detached; new-tab connection timed out); earlier rendered evidence remains valid for the unchanged frontend. Validate screen-reader delivery and any event-specific accessible PDF requirement; the local print export is not certified for it.

IAM scope: App Runner trusts only `tasks.apprunner.amazonaws.com`; image pull uses a distinct `build.apprunner.amazonaws.com` role. Data API permissions name one cluster, secret access names one ARN, Runtime/Gateway calls name their configured resources. Model ARN wildcards allow the configurable five-phase model set and cross-region inference profiles; that is intentionally broader than one fixed model. Runtime endpoint suffixes allow endpoint versions. Generated logging/telemetry and ECR authorization actions may need resource wildcards where the service does not support resource-level authorization. No new deployer grant, permission boundary, or service-linked role was created or simulated. Restrict model selection further for a fixed production workload.

Current AWS claims were checked against primary sources: [Policy permissions](https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/policy-permissions.html), [AgentCore authorization actions](https://docs.aws.amazon.com/service-authorization/latest/reference/list_bedrock-agentcore.html), [policy concepts](https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/policy-core-concepts.html), [supported code runtimes](https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/runtime-code-deploy-supported-runtimes.html), [Data API timeouts](https://docs.aws.amazon.com/AmazonRDS/latest/AuroraUserGuide/data-api-timeouts.html), and [Secrets Manager agent-safe execution](https://docs.aws.amazon.com/secretsmanager/latest/userguide/retrieving-secrets-ai-agents.html). These sources establish documented behavior, not this deployment's runtime permissions.

## Source publication

The intended source target is `origin/main`, without force-push. All task-related implementation, tests, documentation and presentation assets belong in the source commit. Exact staged review, clean-checkout checks, final push and independent remote verification are recorded during finalization. The final commit identity is the containing Git revision, obtainable with `git rev-parse HEAD`; the final handoff supplies its full SHA and an independent `git ls-remote origin refs/heads/main` result. No Workshop Studio repository or publication operation applies.
