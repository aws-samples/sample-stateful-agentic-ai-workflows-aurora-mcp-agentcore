/** Travel symbols from the same Lucide family as the rest of the app. */
export {
  BellRing as ConciergeBell,
  Ticket as BoardingPass,
  ClipboardList as Itinerary,
  Route as RouteMark,
} from 'lucide-react';
